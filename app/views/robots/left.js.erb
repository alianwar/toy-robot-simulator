<% if @robot.valid? %>
  var text = '<%= @robot.report %>'
  Swal.fire({ icon: 'success', title: 'Turned Left', text: text })
<% else %>
  Swal.fire({ icon: 'error', text: 'Invalid Move' })
<% end %>
