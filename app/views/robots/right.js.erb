<% if @robot.valid? %>
  var text = '<%= @robot.report %>'
  Swal.fire({ icon: 'success', title: 'Turned Right', text: text })
<% else %>
  Swal.fire({ icon: 'error', text: 'Invalid Move' })
<% end %>
